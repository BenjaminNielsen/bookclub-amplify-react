type UserBooks @model @auth(rules: [{allow: owner}]) {
  id: ID!
  isbn: String
  title: String
  author: [String]
  genre: [String]
  numberInSeries: String
  wordCount: Int
  description: String
  rating: Int
  progress: Int
  dateStarted: AWSDate
  dateFinished: AWSDate
}

type SuggestionBooks @model @auth(rules: [{allow: public}, {allow: private}]) {
  id: ID!
  isbn: String
  title: String
  author: [String]
  genre: [String]
  numberInSeries: String
  wordCount: Int
  description: String
}
 